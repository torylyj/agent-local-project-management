---
slug: agent-local-project-management
version: 1.4.1
displayName: Agent 本地项目管理
summary: 让本地 Agent 把散落的对话自动整理成项目管理面板：扫描对话目录 → 填写 data.json → 自动渲染统计、时间线与选题，支持云端状态同步与三层自动更新保障，附校验器、实战部署要点与避坑指南。
description: 数据驱动的项目管理模板。本地 Agent 扫描自己的对话目录，按 schema 填写 data.json，页面自动渲染项目统计、Token 月度消耗、项目时间线（点击卡片展开介绍/完成标准/推进建议/产出文件）、创作选题与 AI 新闻。进阶能力（v1.4）：全局搜索（⌘K）、看板视图、年份筛选 + 倒序时间线、紧急程度标记、创作中心（灵感「想做/已做/放弃」+ 新闻收藏/已读）、详细周报（复制/下载）、JSON 导入导出；状态与进度可通过本地 Hook 服务云端同步（/api/state），并由「每日定时 + 开机补跑 + 活动监测看门狗」三层机制保证每次任务收尾自动更新。内置 validate.py 校验器、数据源状态条、删除撤销、多端适配与深色模式。
tags: [project-management, agent, dashboard, template, data-driven, productivity]
license: MIT
---

<div align="center">

# 🗂️ Agent 本地项目管理

> 让本地 AI Agent 把散落的对话，自动整理成一张清晰的项目管理面板。

`数据驱动` · `开箱即用` · `云端同步` · `自动更新`

![Version](https://img.shields.io/badge/version-1.4.1-339CFF) ![License](https://img.shields.io/badge/license-MIT-22c55e) ![Status](https://img.shields.io/badge/status-stable-924ff7)

</div>

---

## 📑 目录

1. [这个技能是什么](#-这个技能是什么)
2. [适用场景](#-适用场景)
3. [使用前准备](#-使用前准备)
4. [快速开始（3 步）](#-快速开始3-步)
5. [详细使用流程（6 步）](#-详细使用流程6-步)
6. [维护约定（重点）](#-维护约定重点)
7. [实战沉淀（v1.4+）](#-实战沉淀v14)
8. [基线模板 vs 完整功能版](#-基线模板-vs-完整功能版)
9. [常见问题 FAQ](#-常见问题-faq)
10. [文件清单](#-文件清单)

---

## 🎯 这个技能是什么

一个**数据驱动的项目管理模板**：你只需要让 Agent 扫描对话目录、把结果写进 `data.json`，
页面就会自动生成统计、时间线、选题和新闻——**刷新即可更新，完全不用改页面代码**。

核心价值：

- 对话太多、项目进度混乱 → 一张面板看清所有项目做到哪一步
- 完成度没有标准 → 每个项目强制定义「100% 的定义」，严格闭环
- 维护靠自觉 → 三层自动更新保障，任务收尾自动同步

## ✅ 适用场景

- 日常用 AI 对话推进多个项目、需要跟踪进度的个人用户
- 想给本地 Agent 建立「项目归集 + 进度维护」习惯的开发者
- 需要把进度可视化发给老师 / 伙伴 / 甲方的场景

## 🔧 使用前准备

| 需要 | 说明 |
| --- | --- |
| Python 3 | 运行校验器与本地预览（`python3`） |
| 本地 Agent（Codex 等） | 负责扫描对话目录并填写 `data.json` |
| 浏览器 | 打开面板页面（建议 Chrome / Safari） |

> 💡 进阶（可选）：完整功能版还需要一个本地 Hook 服务与自定义域名，见 [实战沉淀 §7.1](#-实战沉淀v14)。

## 🚀 快速开始（3 步）

```bash
# ① 复制空模板为你的数据文件
cp data.example.json data.json

# ② 想先看完整效果？用演示数据（可选）
cp demo.json data.json

# ③ 启动本地服务，打开面板
python3 -m http.server 8000
# 浏览器访问 http://localhost:8000
```

> ⚠️ **注意**：直接双击 `template.html` 只会显示**示例数据**（页面顶部有橙色警示条）。
> 真实数据必须通过 http 服务访问，因为浏览器禁止 `file://` 下读取 `data.json`。

## 📖 详细使用流程（6 步）

| 步骤 | 做什么 | 产出 |
| --- | --- | --- |
| ① 扫描对话目录 | Agent 遍历工作区（如 `Documents/Codex/<日期>/<会话>`），读取每个会话的 `outputs/` 或产出文件 | 每个对话「做了什么」的判断 |
| ② 合并成项目 | 把主题相关的多个对话归为一个项目 | 项目清单（名称 / 日期 / 分类 / 状态 / 进度 / Token） |
| ③ 写完成标准 | 为每个项目定义「100% 的定义」（`criteria`） | 严格闭环标准 |
| ④ 填写数据 | 把扫描结果写入 `data.json`（schema 见 [README.md](README.md)） | 可渲染的数据文件 |
| ⑤ 运行校验器 | `python3 validate.py data.json`，必须 **0 错误** | 合格的数据文件 |
| ⑥ 打开面板 | 启动本地服务访问页面 | 可视化的项目管理面板 |

> 📌 **严格标准**：完成标准未全部达成的项目一律不算「已完成」——宁可低估，不虚报。
> 无产出的空会话归入 `empty`，不计入项目。

## 🔄 维护约定（重点）

**每次任务更新或收尾时，由 Agent 执行：**

1. 扫描工作区，找出受影响的对话
2. 更新 `data.json` 中对应项目的状态 / 进度 / 里程碑 / 下一步建议
3. 运行 `python3 validate.py data.json` 确认 0 错误
4. 保存 `data.json`（页面刷新即更新，无需重新部署）

> ✍️ 这条约定可直接写进项目的 `AGENTS.md`，让 Agent 每次收尾自动维护面板。
> 更进一步的三层兜底机制（全局规则 / 看门狗 / 每日定时）见下方 [实战沉淀 §7.2](#-实战沉淀v14)。

---

## 🧠 实战沉淀（v1.4）

> 以下内容来自真实落地「项目管理面板」的完整流程。部署到自己的域名后，把
> 「Hook 域名 / 静态域名」替换为你自己的地址即可。

### 7.1 云端状态同步（进阶，可选）

模板默认把状态/进度等编辑保存在浏览器本地；若页面运行在**沙箱 iframe**（如渲染工具生成的 srcdoc）中，`localStorage` 不可用，必须改为云端同步：

- 本地起一个 Hook 服务（`127.0.0.1:8787`），提供 `GET/POST /api/state`，把状态落盘成 `state.json`：

  | 字段 | 含义 |
  | --- | --- |
  | `urgency` | 项目紧急程度 `{项目id: 0|1|2}` |
  | `topicMarks` | 灵感标记 `{选题: todo|done|skip}` |
  | `newsFavs / newsRead` | 新闻收藏与已读 |
  | `projects` | `{项目id: {status, progress}}` |

- 页面加载采用**双通道**：先用线上静态 `state.json` 秒开应用（可能略旧），再请求 Hook 权威接口覆盖，保证「快」且「最终一致」。
- 项目卡片上的紧急程度旗标、详情里的状态/进度修改，改动即 `POST /api/state` 同步。

### 7.2 自动更新保障（三层兜底）

「每次任务更新或收尾自动更新面板」不能只靠 agent 自觉，需要三层机制：

1. **规则层**：维护约定写进 `~/.codex/AGENTS.md`（全局，对所有 Codex 对话生效）+ 项目工作区 `AGENTS.md`（项目级优先级更高）。
2. **看门狗层**：本地服务每 N 分钟（默认 10 分钟）扫描对话目录集合，发现**新的对话目录/产出**且距上次深度更新超过冷却时间（默认 60 分钟），自动触发一次完整更新。
3. **定时层**：每日固定时间（默认 09:00）深度更新；到点没开机则开机后立即补跑。

> ✅ 结果：正常使用下，新对话开工或任务收尾后 **≤10 分钟内**自动反映到线上，最迟次日定时任务兜底。

### 7.3 部署实战要点

- **沙箱 iframe 里相对路径 fetch 会失效**：页面内所有请求（新闻、更新记录、状态）必须写**绝对地址**，否则永远走不到线上 JSON。
- **静态托管要配 CORS**：沙箱 iframe 跨域读 JSON 需要 `Access-Control-Allow-Origin: *`。R2 桶可用 S3 `PutBucketCors` 配置；否则 fetch 被浏览器拦截、静默回退到内置数据。
- **上传脚本加重试**：静态上传（R2 等）偶发 SSL/连接中断，逐对象重试 3 次（捕获全部异常）即可保证部署稳定。
- **渲染后处理**：渲染完记得注入 favicon，并把 CSP `connect-src` 补上 hook/静态域名（否则跨域请求全被 CSP 拦截）。
- **隧道稳定性**：公网隧道（cloudflared 等）偶发抖动，页面请求失败时会提示「服务器未连接」，重试即可恢复；可考虑加备用线路。

### 7.4 已知坑（先看再动手）

- 🪟 沙箱 iframe 中 `localStorage` 不可用 → 所有需要持久化的状态走云端同步。
- 🖨️ 沙箱 iframe 中 `window.print()` 可能被拦截 → 导出 PDF 不可靠，报告用**复制文本 + 下载 .txt**。
- 📅 时间线月份若改成「1-12 月」，项目月份下标要按真实日期推导，否则全部错位；空月份不展示、月份倒序展示更符合「最近优先」。
- 📊 **云端状态同步后必须重算统计**：`applyState` 应用服务器端 `projects` 状态后，要调用 `renderStats()` 重算顶部「已完成 / 进行中」统计卡片；否则时间线显示「已完成」、顶部统计却不更新（2026-08 发现并修复，已纳入本技能）。
- ⚡ 状态加载要防慢隧道：Hook 接口偶发慢响应，页面应采用「静态快照秒开 + Hook 权威覆盖」双通道，避免统计长时间停留在旧值。

### 7.5 报告导出

「分享周报」为详细结构化周报：本周概览 / 项目明细（状态、进度、下一步、完成标准、里程碑）/ 优先推进 / 创作灵感与 AI 动态 / 数据说明，支持一键复制与下载 .txt。

### 7.6 Token 用量绑定（随更新自动回写）

项目 Token 不再停留在旧快照：`update_tokens.py` 读取 Codex 会话文件
（`~/.codex/sessions/**/*.jsonl`）的 `payload.info.total_token_usage`，按会话工作目录归属到项目
（会话 cwd 与项目 dir 互相包含即匹配），以「输入 + 缓存输入 + 缓存写入 + 输出 + 推理」口径
回写源文件各项目的 `tokens` 字段，并同步聚合月度用量。接入更新流程后，每次「文档更新」都会同步刷新 Token。

```bash
python3 update_tokens.py <面板源文件.html>   # 会话目录默认 ~/.codex/sessions
```

> 📌 脚本输出的「未归属」目录仅作提示，是否新建项目由用户决定（避免自动重建已删除的项目）。

---

## 📊 基线模板 vs 完整功能版

`template.html` 是**轻量基线版**（数据驱动，开箱即用）：统计、Token 月度消耗、按年/月时间线、选题与新闻、状态/进度编辑、删除空对话。

**完整功能版**（文档 v1.4 描述的能力）还包含：全局搜索（⌘K）、看板视图、紧急程度标记、创作中心（灵感标记 + 新闻收藏/已读）、详细周报、JSON 导入导出、云端状态同步（`/api/state`）、Token 用量自动回写与三层自动更新保障。这些能力依赖**本地 Hook 服务 + 自定义域名**，属于进阶部署，因此不并入基线模板。

**升级路径**：先用基线模板跑通「扫描 → 填 data.json → 出面板」；需要进阶能力时，按「实战沉淀」章节接入 Hook 服务、`update_tokens.py` 与看门狗，即可获得完整版体验。

---

## ❓ 常见问题 FAQ

<details>
<summary><b>打开页面显示橙色「示例数据」警示条？</b></summary>

说明 `data.json` 没被读到。检查：① 文件是否已创建；② 是否通过 `python3 -m http.server` 访问而不是直接双击。

</details>

<details>
<summary><b>页面数据不是我改的内容？</b></summary>

本地编辑会带时间戳；如果 `data.json` 的 `updatedAt` 更新，页面会以数据文件为准，避免旧修改覆盖新数据。

</details>

<details>
<summary><b>删除空对话后想恢复？</b></summary>

删除后 6 秒内点 toast 上的「撤销」即可。

</details>

<details>
<summary><b>如何发布到线上？</b></summary>

面板是纯静态文件，可部署到任意静态托管（GitHub Pages / Cloudflare R2 / Vercel 等），同时上传 `template.html`、`data.json` 即可。

</details>

<details>
<summary><b>为什么我改了状态/进度，刷新后丢了？</b></summary>

页面若运行在沙箱 iframe 中，浏览器本地存储不可用。需要按「实战沉淀 §7.1」部署本地 Hook 服务做云端同步（`/api/state`），并把状态文件随站点发布。

</details>

<details>
<summary><b>时间线显示已完成，顶部统计却没变？</b></summary>

这是「云端状态同步后未重算统计」的典型表现，已修复并纳入技能：应用服务器状态后会自动重算顶部统计卡片（见 §7.4）。

</details>

<details>
<summary><b>为什么「导出 PDF」没有反应？</b></summary>

沙箱 iframe 中的 `window.print()` 可能被浏览器拦截，PDF 导出已移除。周报请用「复制内容」或「下载 .txt」。

</details>

<details>
<summary><b>新对话开工后，面板多久会自动更新？</b></summary>

看门狗默认 10 分钟一轮：检测到新的对话目录/产出后自动触发完整更新（冷却 60 分钟）；电脑关机期间则等开机补跑，最迟次日定时任务兜底。

</details>

---

## 📁 文件清单

| 文件 | 用途 |
| --- | --- |
| `SKILL.md` | 本说明文档（使用指南） |
| `README.md` | 制作流程蒸馏 + 数据口径 + 版本记录 |
| `template.html` | 面板页面（基线版 v1.3，数据驱动；完整功能版见「基线 vs 完整版」） |
| `validate.py` | data.json 校验器 |
| `data.example.json` | 空白数据模板 |
| `demo.json` | 完整演示数据 |
| `update_tokens.py` | Token 用量自动回写（可选，进阶） |

---

<div align="center">

**Agent 本地项目管理 · v1.4.1** · 数据驱动 · 严格标准 · 自动维护

</div>
